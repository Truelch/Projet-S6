#include "MapTile.h"

//  LA CLASSE TILE EXISTE DEJA DANS COCOS2D 

MapTile::MapTile(float x, float y, const char * filename, Scene * scene, Layer * layer): Displayable(x,y,0,filename,scene,layer)
{
	//
}
